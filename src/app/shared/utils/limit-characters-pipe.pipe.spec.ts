import { LimitCharactersPipePipe } from './limit-characters-pipe.pipe';

describe('LimitCharactersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitCharactersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
